# k8s/02-app-secret.yaml
# Gestiona los datos sensibles, como claves de API o credenciales.
# El valor de JWT_KEY está codificado en Base64.
# Para generar un nuevo valor: echo -n 'tu-clave-secreta' | base64
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: microservices-ns
type: Opaque
data:
  # Valor de 'myfancysecret' codificado en Base64
  JWT_SECRET: bXlmYW5jeXNlY3JldA==
